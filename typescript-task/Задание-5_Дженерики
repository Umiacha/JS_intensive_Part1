/*
    Реализуйте функцию first, которая возвращает первый элемент массива любого типа.
    Если массив пустой — возвращайте null.
    Используйте дженерики для типизации функции.
*/

export function first(/* ваш код здесь */) {
    // ваш код здесь
}

// Примеры использования:
const n = first([1, 2, 3]); // 1
const s = first(['a', 'b', 'c']); // 'a'
const empty = first([]); // null
