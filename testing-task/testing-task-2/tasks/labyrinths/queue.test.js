/**
 * Всего - 4 балла (1 балл - открытые, 3 балла - скрытые)
 *
 * Ещё скрытые тесты:
 * 1. 1 балл
 * 2. 2 балла
 */
describe("Сценарии файла queue", () => {
  // Открытый, 1 балл
  test("Если создали очередь и подобавляли в неё элементы, а затем поисключали все элементы из нее, то возвращаются все элементы в порядке их добавления", () => {});
});
